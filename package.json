{
  "name": "bun-lit",
  "module": "server.js",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "bld": "bun build ./src/server/index.ts --outdir dist; cp ./src/server/public/favicon.ico dist/; cp ./src/server/public/index.html dist/",
    "tslint": "bun tsc --noEmit",
    "start": "bun bld; bun run --watch ./src/server/main.ts"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "lit": "^3.1.3"
  }
}
